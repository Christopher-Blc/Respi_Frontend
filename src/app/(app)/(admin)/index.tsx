import { View, Text } from 'react-native';

export default function indexAdmin() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>Pantalla de index de admin</Text>
    </View>
  );
}