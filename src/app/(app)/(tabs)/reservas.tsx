import { View, Text } from 'react-native';

export default function ReservasClientes() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>Pantalla de RESERVAS de clientes</Text>
    </View>
  );
}