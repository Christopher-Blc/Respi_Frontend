import { View, Text } from 'react-native';

export default function PistasClientes() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>Pantalla de PISTAS de clientes</Text>
    </View>
  );
}