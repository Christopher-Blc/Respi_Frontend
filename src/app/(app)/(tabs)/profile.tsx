import { View, Text } from 'react-native';

export default function ProfileClientes() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>Pantalla de PERFIL de clientes</Text>
    </View>
  );
}